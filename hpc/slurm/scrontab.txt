# ============================================================================
# scrontab is DISABLED on LANTA — verified July 2026:
#   $ scrontab
#   scrontab: fatal: scrontab is disabled on this cluster
#
# Resubmission therefore runs from the ALWAYS-ON APP VM instead (which the
# architecture already allows: "scrontab or Prefect from the app VM").
# Cron entry on the app VM — every 15 min, resubmit the serving job if absent:
#
#   */15 * * * * ssh -i <deploy_key> <user>@lanta.nstda.or.th \
#       'bash /project/tn999991-cstu/chin/check_and_resubmit.sh'
#
# check_and_resubmit.sh (lives on LANTA): checks `squeue --me` for the
# m3-vllm-serve job by name and sbatches serve_vllm.sbatch if it is not
# queued or running. tunnel.sh on the app VM then reconnects to the new node.
#
# *** OPEN BLOCKER: LANTA requires password + Google 2FA on EVERY SSH
# connection. Until ThaiSC confirms a deploy-key exemption for non-interactive
# access (asked: thaisc-support@nstda.or.th), unattended cron CANNOT log in,
# and resubmission + tunnel reopening remain MANUAL steps. ***
# ============================================================================
