[project]
name = "mission3-db"
version = "0.1.0"
description = "Mission 3 database migrations (Alembic)"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.13",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "pgvector>=0.3",
]

[tool.uv]
package = false
